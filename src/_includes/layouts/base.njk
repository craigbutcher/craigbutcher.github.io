<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
      {# {{ renderData.title or title }} {{ metadata.title }} #}
      {% set pageTitle = title + ' - ' + metadata.title %}
      {{ pageTitle }}

    </title>
    <meta name="Description" content="{{ renderData.description or description or metadata.description }}">

    <link href="https://fonts.googleapis.com/css?family=IBM+Plex+Sans+Condensed:600|Open+Sans&display=swap" rel="stylesheet">

    <link rel="stylesheet" href="{{ '/assets/css/style.css' | url }}">
    <link rel="alternate" href="{{ metadata.feed.path | url }}" type="application/atom+xml" title="{{ metadata.title }}">

  </head>
  <body>

    <div class="grid">
    <header class="one">
        <h1 class="home"><a href="{{ '/' | url }}">{{ metadata.title }}</a>
            <!-- <small> - I <span class="heart">❤</span> Hannah</small></h1> -->
      <h2>Front End Web Developer</h2>

      {# {{ collections.all | eleventyNavigation | eleventyNavigationToHtml({
        listClass: "nav",
        listItemClass: "nav-item",
        activeListItemClass: "nav-item-active",
        activeKey: eleventyNavigation.key
      }) | safe }} #}

      {% set navPages = collections.all | eleventyNavigation %}

      <nav>
        <ul>
        {%- for entry in navPages %}
            <li{% if entry.url == page.url %} class="navigation-active"{% endif %}>
              <a href="{{ entry.url | url }}" aria-current="{{ entry.title }}">{{ entry.title }}</a>
            </li>
        {%- endfor %}
            <li><a href="https://twitter.com/craigbutcher">Twitter</a></li>
        </ul>
      </nav>

    </header>

    <main{% if templateClass %} class="{{ templateClass }} eleven"{% endif %}>
      {# <div class="warning">
        <ol>
          <li>Edit the <code>_data/metadata.json</code> with your blog’s information.</li>
          <li>(Optional) Edit <code>.eleventy.js</code> with your configuration preferences.</li>
          <li>Delete this message from <code>_includes/layouts/base.njk</code>.</li>
        </ol>
        <p><em>This is an <a href="https://www.11ty.io/">Eleventy project</a> created from the <a href="https://github.com/11ty/eleventy-base-blog"><code>eleventy-base-blog</code> repo</a>.</em></p>
      </div> #}

      {{ content | safe }}

    </main>

    {# <footer></footer> #}

    {# Current page: {{ page.url | url }} #}
    </div>
  </body>
</html>
